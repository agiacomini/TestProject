package com.giacomini.andrea.BuildingBlocks.ExamEssentials;

public class Notes {

	/*
	 * N.B: Note per l'esame -
	 * 		- saper scrivere il metodo main();
	 * 		- conoscere gli effetti dell'uso del package e dell'import;
	 * 		- riconoscere un costruttore;
	 * 		- identificare dichirazioni e inizailizzazioni di variabili;
	 * 		- determinare lo scope di una variabile;
	 * 		- riconoscere gli statement malposti in una classe. L'ordine degli elementi (package, import, class - PIC);
	 * 		- identificare un oggetto eleggibile per il garbage collector;
	 */

	protected static void main(String[] name){}
}
