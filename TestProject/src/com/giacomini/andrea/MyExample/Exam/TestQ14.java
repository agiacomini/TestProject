package com.giacomini.andrea.MyExample.Exam;

public class TestQ14 {

	/*
	 * Il metodo "method()" è marcato con un livello di accesso PUBLIC (il meno restrittivo), il che significa 
	 * che è richiamabile da chiunque. Inoltre, la parola chiave "void" indica che non ritorna nessun valore infatti
	 * non si trova, nel corpo del metodo, la parola chiave "return" che serve proprio per indicare quale valore restituire.
	 * Naturlamente il metodo, quando richiamato, si aspetta di avere in input un argomento di tipo "int".
	 * Il metodo è un metodo di istanza (non c'è la parola chiave STATIC che lo precede), il che significa che è richiamabile
	 * solo nel caso in cui si ha un'istanza della classe che lo racchiude. 
	 */
	public void method(int i){
		
	}
}
