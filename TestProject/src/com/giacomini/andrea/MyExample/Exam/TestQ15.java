package com.giacomini.andrea.MyExample.Exam;

public class TestQ15 {

	/*
	 * Nel caso in cui si omette il modificatore di accesso (che generalmente si esplicita all'inizio della firma)
	 * viene assegnato automaticmanete quello di default.
	 * La parola chiave "static" NON è usata come modificatore di accesso. Serve solo per indicare/etichettare un metodo 
	 * (oppure una variabile) di Classe che significa che non è necessario avere un'istanza della classe che lo racchiude
	 * per poterlo usare. 
	*/
	static int min(double[] in){
		
		// ::code
		int i = 0;
		return i;
	}
}
