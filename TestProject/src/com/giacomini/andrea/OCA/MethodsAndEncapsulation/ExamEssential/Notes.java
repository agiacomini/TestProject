package com.giacomini.andrea.OCA.MethodsAndEncapsulation.ExamEssential;

public class Notes {

	/*
	 * N.B: Note per l'esame:
	 * 			- Identificare dichirazione di metodi corrette;
	 * 			- Identificare quando un campo o un metodo è accessibile;
	 * 			- Riconoscere quando è necessario uno "STATIC IMPORT";
	 * 			- Sapere riconoscere quando viene chimato un membro (campo o metodo) d'istanza oppure un membro statico;
	 * 			- Riconoscere l'overloading dei metodi e l'ordine di esecuzione (match corretto, primitiva più grande, autoboxing, varargs);
	 * 			- Assegnare un nuovo valore ai paramtri del metodo non cambia il valore il delle variabili passate dal chiamante;
	 * 			- L'uso dei costruttori;
	 * 			- Un costruttore può richiamare un'altro costruttore usando la parola chiave "this.";
	 * 			- Ricordare l'ordine di inizializzazione: superclass, variabili static, variabili d'istanza, il costruttore;
	 * 			- Riconoscere quando una classe è incapsulata: le variabili d'istanza private, e metodi getter/setter public;
	 * 			- Saper scrivere "Lambda Expression": saper identificare la presenza o assenza di elementi opzionali;
	 * 			- Lambda Expression: il tipo dei parametri è opzionale, 
	 * 								 le parentesi graffe e il "return statement" sono opzionali se il corso della lambda expression è composta da un solo statement;
	 * 								 le parentesi tonde sono opzionali quando la lista dei parametri ha un solo elemento e non è specificato il tipo del parametro;
	 * 								 l'interfaccia "Predicate" è spesso usata con le lambda expression perché dichiara un singolo metodo "test()" il quale prende un prametro solo;
	 * 			     
	 */
}
